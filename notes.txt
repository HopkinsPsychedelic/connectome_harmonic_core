Generating neurodocker file command:
Brian test
docker run --rm repronim/neurodocker:0.7.0 generate docker --base=debian:stretch --pkg-manager=apt \
  --miniconda create_env=neuro \
              conda_install='python=3.6 time itertools numpy librosa math random ast statistics ast pandas os jupyter jupyterlab jupyter_contrib_nbextensions' \
              pip_install='meshio nibable vtk scipy sklearn nilearn git+https://github.com/enthought/mayavi.git' \
  --install git vim nano \
  --freesurfer version=6.0.0 method=binaries --copy license.txt /opt/freesurfer-6.0.0/ \
  --fsl version=5.0.10 method=binaries \
  --mrtrix3 version=3.0_RC3

Packages needed: libapt-inst, libapt-pkg, libbz, libcurl3, 

!pip3 install meshio nibabel vtk scipy sklearn plotly==4.9.0 kaleido==0.0.1 nilearn
!pip3 install git+https://github.com/enthought/mayavi.git
free surfer, fsl, mrtrix 


import meshio
import nibabel as nib
import scipy
import sklearn.neighbors as skn
import vtk
from scipy import sparse
from scipy.sparse import csgraph
from tvtk.api import tvtk, write_data
import matplotlib.pyplot as plt
from matplotlib.pyplot import figure
from sklearn.metrics import mean_squared_error
import pandas as pd
import nilearn
from nilearn import image

